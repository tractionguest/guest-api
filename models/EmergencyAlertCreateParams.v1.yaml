title: EmergencyAlertCreateParams
type: object
x-examples:
  default:
    message: some text
    channels:
      - SMS
      - EMAIL
description: ''
properties:
  channels:
    description: "Specify the broadcast channel, one of 'SMS' and/or 'EMAIL'"
    type: array
    items:
      type: string
      enum:
        - SMS
        - EMAIL
      example: '["SMS", "EMAIL"]'
  message:
    type: string
    description: Plain text of the alert to be sent
    example: some text
required:
  - channels
  - message
x-tags:
  - EmergencyAlerts
