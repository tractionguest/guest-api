title: GuestAlertCreateParams
type: object
x-examples:
  default:
    message: some text
    channels:
      - SMS
      - EMAIL
    signin_selectors:
      is_signed_out: true
      signin_ids:
        - 45
        - 91
      location_ids:
        - 46
        - 83
description: ''
properties:
  signin_selectors:
    $ref: ./GuestAlertSigninSelectors.v1.yaml
  channels:
    description: "Specify the broadcast channel, one of 'SMS' and/or 'EMAIL'"
    type: array
    items:
      type: string
  message:
    description: Plain text of the alert to be sent
    type: string
required:
  - channels
  - message
