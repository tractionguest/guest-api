title: Location
description: "The root of the Location type's schema."
required:
  - id
  - name
type: object
properties:
  id:
    format: int32
    type: integer
  name:
    type: string
x-examples:
  default:
    id: 1
    name: Default
    max_capacity: 30
