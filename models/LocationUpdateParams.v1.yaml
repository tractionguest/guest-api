title: LocationUpdateParams
type: object
x-tags:
  - Locations
x-examples:
  default:
    max_capacity: 30
    timezone: 'Central Time (US & Canada)'
properties:
  max_capacity:
    type: integer
  timezone:
    type: string
