title: LocationUpdateParams
type: object
x-tags:
  - Locations
x-examples:
  default:
    max_capacity: 30
    timezone: America/Vancouver
properties:
  max_capacity:
    type: integer
  timezone:
    type: string
    description: 'IANA timezone designations is followed here, since that’s the only reliable way to maintain consistent time throughout the year'
