title: SigninData
type: object
x-examples:
  default:
    name: some text
    title: some text
    custom_fields:
      - format: string
        field_name: some text
        field_value: some text
      - format: string
        field_name: some text
        field_value: some text
properties:
  custom_fields:
    type: array
    items:
      $ref: ./CustomField.v1.yaml
  name:
    type: string
  title:
    description: Title of the sign-in flow page that captured this entry, as configured in the workflow. Null when the page has none.
    type: string
    nullable: true
